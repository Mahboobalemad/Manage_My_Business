# مواصفات بناء نظام إدارة محل تجاري (C# / WPF / SQLite)

> **تعليمات للمنفذ (Antigravity):** ابنِ النظام على المراحل المرقّمة أدناه بالترتيب. **بعد كل مرحلة توقف تمامًا**، واعرض ملخصًا مختصرًا لما تم إنجازه + أي قرارات تقنية اتخذتها + الملفات التي أنشأتها، وانتظر الموافقة قبل الانتقال للمرحلة التالية. لا تنتقل لمرحلة تالية دون تأكيد صريح.

---

## 1. نظرة عامة على النظام

تطبيق سطح مكتب لإدارة محل تجاري (فرع واحد أو أكثر)، لمستخدم واحد (المالك)، يعمل بدون إنترنت (Offline-First)، مع دعم نسخ احتياطي اختياري (محلي أو سحابي عبر Google Drive).

**ليس** نظام ERP ولا محاسبة قيد مزدوج — هو دفتر إدارة إلكتروني مبسّط.

## 2. القواعد المحاسبية الأساسية (يجب الالتزام بها حرفيًا)

```
صافي مبيعات اليوم = إجمالي المبيعات − إجمالي المصروفات
```
- الفواتير (بضاعة مستلمة) **لا تُحسب أبدًا** ضمن صافي المبيعات اليومي.
- لا بيع آجل، لا تسجيل عميل فردي — المبيعات تُسجَّل كرقم إجمالي يومي واحد لكل فرع.

```
رصيد التاجر المستحق = مجموع الفواتير − مجموع الدفعات
```
- الفاتورة (حركة +) والدفعة (حركة −) مستقلتان تمامًا، تُسجَّلان كحركتين منفصلتين في كشف حساب التاجر (Ledger).

```
مستحق الموظف = الرواتب + المكافآت − السحبيات − الخصومات 
```
يمكن اصدار التقارير من صفحة ا
## 3. التقنيات

- **اللغة:** C#
- **الواجهة:** WPF (.NET 8) — دعم RTL كامل + Dark Mode
- **قاعدة البيانات:** SQLite (عبر Entity Framework Core Code-First، لتسهيل الانتقال لاحقًا لـ PostgreSQL)
- **التقارير:** QuestPDF (تصدير PDF) + ClosedXML (تصدير Excel)
- **النسخ السحابي:** Google Drive API (OAuth بحساب المالك الشخصي)
- **البنية:** Clean Architecture (فصل: Data / Domain-BusinessLogic / UI) لسهولة التوسع مستقبلًا

## 4. مخطط قاعدة البيانات (الجداول الأساسية)

- `Branches` (الفروع): Id, Name, Notes
- `DailyActivity` (النشاط اليومي): Id, BranchId, Date, TotalSales, TotalExpenses, Notes
- `ExpenseItems` (تفاصيل مصروف اليوم — اختياري تفصيلي): Id, DailyActivityId, Category, Amount
- `Invoices` (فواتير البضاعة): Id, BranchId, TraderId, Date, InvoiceName, Amount, Notes
- `Traders` (التجار): Id, Name, Phone, Notes
- `TraderTransactions` (حركات حساب التاجر): Id, TraderId, Date, Type(فاتورة/دفعة), Amount, RelatedInvoiceId(nullable)
- `Employees` (الموظفون): Id, Name, Phone, Position, BranchId, Salary, HireDate, Status, Notes
- `EmployeeTransactions` (حركات الموظف): Id, EmployeeId, Date, Type(راتب/مكافأة/سحبية/خصم/دفعة), Amount, Notes
- `ZakatEntries` (**تبويب الزكاة — مستقل تمامًا**): Id, Date, Amount, ZakatType, Notes
- `BackupLog`: Id, Date, Type(محلي/سحابي), FilePath\_أو\_DriveFileId, Status

## 5. التبويبات المطلوبة في الواجهة

1. لوحة التحكم (ملخص يومي/شهري + فلترة حسب الفرع/الفترة)
2. الفروع (إضافة/تعديل)
3. النشاط اليومي (مبيعات + مصروفات لكل فرع، جدول بالشكل: تاريخ | مبيعات | مصروفات | صافي | فواتير | ملاحظات) 
4. فواتير البضاعة
5. التجار (قائمة + كشف حساب لكل تاجر)
6. الموظفون (قائمة + حركات مالية لكل موظف)
7. **الزكاة (تبويب مستقل جديد)** — جدول: التاريخ | المبلغ | نوع الزكاة | ملاحظات + إجمالي أسفل الجدول (Sum تلقائي)، بدون أي ربط مع باقي التبويبات
8. التقارير (فروع / تجار / موظفين، تصدير PDF و Excel)
9. النسخ الاحتياطي — **يختار المستخدم في كل مرة: نسخ محلي أو نسخ سحابي (Google Drive)**، وكذلك عند الاستعادة يختار المصدر
10. الإعدادات (Dark Mode، إلخ)
لكل محل او موظف جدول خاص بكل شهر, يظهر كتبويب رئيسي في راس الجدول :يناير | فبراير|... تحت هذا التبويب تكون التبويبات الخاصة الموضحة اعلاه سواء للمحل او الموظف مع اجمالي تلقائي لكل شهر

## 6. ما لا يُطلب في هذه النسخة (تجنّب بناءه)

حسابات عملاء، بيع آجل، تعدد مستخدمين/صلاحيات، محاسبة قيد مزدوج، POS تفصيلي، مخزون تفصيلي لكل منتج، تعدد عملات، تطبيق جوال، مزامنة سحابية حية (فقط نسخ احتياطي عند الطلب).

---

## خطة المراحل (توقف بعد كل مرحلة وقدّم تقريرًا)

**المرحلة 1 — الأساس:** إعداد المشروع (.NET 8 WPF)، هيكلة المجلدات (Clean Architecture)، إعداد SQLite + EF Core، إنشاء كل الجداول أعلاه (Migrations)، بدون أي واجهة بعد.

**المرحلة 2 — الفروع + النشاط اليومي:** الواجهات والمنطق البرمجي لإدارة الفروع، وتسجيل المبيعات/المصروفات اليومية، وحساب الصافي تلقائيًا.

**المرحلة 3 — الفواتير + التجار:** تسجيل الفواتير، ربطها بالتاجر، حركات التاجر، كشف الحساب، حساب الرصيد المستحق.

**المرحلة 4 — الموظفون:** إدارة الموظفين، تسجيل الرواتب/المكافآت/السحبيات/الخصومات/الدفعات، حساب المستحق.

**المرحلة 5 — تبويب الزكاة:** جدول مستقل (تاريخ، مبلغ، نوع، ملاحظات) + إجمالي تلقائي. وحدة معزولة تمامًا عن بقية النظام.

**المرحلة 6 — لوحة التحكم + التقارير:** ملخصات يومية/شهرية، فلترة، تصدير PDF/Excel لكل التقارير المذكورة في القسم 20 من الوثيقة الأصلية.

**المرحلة 7 — النسخ الاحتياطي:** خيار نسخ محلي (نسخ ملف .db)، وخيار نسخ سحابي (رفع/تنزيل عبر Google Drive API بحساب المالك)، مع سجل للنسخ (BackupLog).

**المرحلة 8 — التلميع النهائي:** Dark Mode، مراجعة RTL الكاملة، اختبار سيناريوهات شاملة (يوم بدون فاتورة، فواتير متعددة، دفعات جزئية)، تجهيز حزمة التثبيت (Installer).

---

**ملاحظة للمنفذ:** عند أي غموض في التفاصيل الدقيقة أثناء التنفيذ، اتبع الأمثلة الرقمية الواردة في وثيقة "تقرير تعريف وتشغيل نظام إدارة محل العبايات" الأصلية كمرجع نهائي للسلوك المتوقع.
